echo 'const express = require("express"); const app = express(); app.get("/", (req, res) => res.send("Bai Tap 1: CI/CD Thanh Cong!")); app.listen(3000, () => console.log("Server ready"));' > index.js

